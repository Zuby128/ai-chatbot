export const successMessage = (item) => {
    return `${item} successfully created`
}

export const updateMessage = (item) => {
    return `${item} successfully updated`
}

export const notFoundMessage = (item) => {
    return `${item} not found or missing value`
}

export const errorMessage = () => {
    return `something went wrong`
}